# Generated icon payload

`lets-icons.json` is generated from the pinned `@iconify-json/lets-icons` npm package.
The generated JSON is embedded into the Razor Class Library and shipped inside the NuGet package.

Run `tools\commands\bootstrap.cmd` on Windows or `./tools/commands/bootstrap.sh` on macOS/Linux.
