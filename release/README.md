# Release artifacts

Validated NuGet packages are written to `release/packages` by:

```powershell
scripts\release\release.cmd 1.0.0
```

Package binaries are build artifacts and are not source files.
